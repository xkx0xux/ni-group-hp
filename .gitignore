# 引き継ぎ・設計メモは公開しない（GitHub repo も ni-holdings.com も公開のため）
# ローカル(~/NIグループHP)には残す。中身＝社内事情・費用・判断経緯
CLAUDE.md
HANDOVER.md
TODO.md
CHANGELOG.md
DESIGN.md
docs/

# 作業中の中間素材（元から未追跡だが明示）
.backup-warm-2026-07-26/
concepts/purple-*.html

# 旧デザインの下書き（氏名・写真を含み、非表示の判断が効かないため公開しない）
concepts/

# 一時非表示中の代表写真（削除禁止・復帰時に git add で戻す）
assets/img/leaders/abe.jpg

# 作業用ファイル（公開不要・サービスIDやメモを含むため）
_tools_*

# その他（公開不要）
.superpowers/
Icon

# Claude Code のプロジェクト設定・作業状態（公開不要）
.claude/

# 代表写真の中間素材（阿部さんの顔を含む・削除禁止だが公開もしない）
# 「まず全部除外 → 公開中の2枚だけ許可」にして、うっかり git add . を防ぐ
assets/img/leaders/*
!assets/img/leaders/kasahara.jpg
!assets/img/leaders/ishiyama.jpg

# 事業写真の中間素材（公開しているのは *-warm.jpg のみ）
assets/img/gen/*
!assets/img/gen/*-warm.jpg
